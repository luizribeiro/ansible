# vim: set ft=yaml:
---
extends: default

rules:
  braces:
    min-spaces-inside: 1
    max-spaces-inside: 1
  truthy:
    check-keys: false

ignore: |
  anstomlog
  spotcast
  secrets.yaml
  library
  .venv
  .cache
